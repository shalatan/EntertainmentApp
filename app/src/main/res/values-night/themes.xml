<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EntertainmentApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!--        Transparent system bars for edge-to-edge-->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <item name="colorSurface">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="bottom_nav_color">@color/black</item>

        <item name="android:fontFamily">@font/libre_font_family</item>
        <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
        <item name="fontFamily">@font/libre_font_family</item>

    </style>

</resources>